<Window x:Class="Zyrenth.OracleHack.Wpf.RingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:local="clr-namespace:Zyrenth.OracleHack.Wpf"
		xmlns:rings="clr-namespace:Zyrenth.OracleHack;assembly=OracleHack"
        Title="RingWindow" Height="300" Width="300">
	
	<Window.Resources>
		<local:BitmapToImageSourceConverter x:Key="BitmapConverter" />
	</Window.Resources>
    <Grid>
		<ListBox x:Name="lstRings" HorizontalAlignment="Left" Height="250" Margin="10,10,0,0" VerticalAlignment="Top" Width="146" ScrollViewer.CanContentScroll="True" >

			<ListBox.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Horizontal">
						<CheckBox />
						<Image Width="18" Height="18" Source="{Binding Path=Image, Converter={StaticResource BitmapConverter}}" />
						<TextBlock Text="{Binding Path=Name}" />
						<StackPanel.ToolTip>
							<TextBlock Text="{Binding Path=Description}" />
						</StackPanel.ToolTip>
					</StackPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>

	</Grid>
</Window>
